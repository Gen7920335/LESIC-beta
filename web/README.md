# Melt / MVS Calibrator Web

Static GitHub Pages version of the PLA Melt/MVS G-code generator.

## Local Run

```powershell
npm install
npm run dev
```

## Build

```powershell
npm run build
```

The GitHub Actions workflow in `.github/workflows/deploy-pages.yml` builds this folder and publishes `web/dist` to GitHub Pages.
